#pragma once
#include<iostream>
using namespace std;

void min(int* a, int size) {
	int max = a[0];
	for (int i = 1; i < size; i++) {
		if (a[i] > max) {
			int temp = max;
			max 
		}
	}
}